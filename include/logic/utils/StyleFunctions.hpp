#ifndef STYLE_FUNCTIONS_HPP
#define STYLE_FUNCTIONS_HPP

/**
 * @brief Simulate the % unity in CSS.
 * @param percentage is the amount of percent of the reference.
 * @param reference is the reference of the percentage.
 * 
 * @return x percent of the reference, being x the percentage.
 */
float percentOf(const float percentage, const float reference);

#endif
